export enum ErrorMessage {
  INVALID_URL = 'Please enter a domain name using one of the following formats: example.com, www.example.com, or http://example.com.',
  ALREADY_BLOCKED = 'is already blocked.',
}
